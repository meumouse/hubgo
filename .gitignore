# Dependencies
node_modules/
app/node_modules
admin/vendor

# Build artifacts are committed (shipped in the plugin zip): app/dist is kept.
# Do NOT add app/dist here — already-tracked bundles would keep working while
# every NEW entry (a new admin page) silently stayed out of the release zip.
# Local tooling
.history/
*.log
app/dist

# OS
.DS_Store
Thumbs.db
.env